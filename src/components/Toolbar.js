import React from 'react';

export default function Toolbar({filters, onSelectFilter, selected}) {
  console.log('filters: ', filters);
  console.log('pronSelectFilterops: ', onSelectFilter);
  console.log('selected: ', selected);
  
  
  
}